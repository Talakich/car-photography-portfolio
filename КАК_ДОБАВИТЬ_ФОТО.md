# 📸 Как добавить фотографии на сайт

## Простой способ - всего 3 шага!

### Шаг 1: Закиньте фото в папку
Просто скопируйте ваши фотографии (JPG, PNG) в папку `images/`

### Шаг 2: Запустите скрипт
Откройте терминал в папке проекта и выполните:
```bash
./update-images.sh
```

### Шаг 3: Готово!
Откройте `index.html` в браузере - все новые фото автоматически появятся в галерее!

---

## Подробнее

**Что происходит:**
- Скрипт `update-images.sh` сканирует папку `images/`
- Создает файл `images/gallery.json` со списком всех фотографий
- JavaScript автоматически загружает фото из этого списка при открытии сайта

**Поддерживаемые форматы:**
- JPG, JPEG
- PNG

**Категории фото:**
Фото автоматически распределяются по категориям (спорткары, классика, премиум, в движении)

**Важно:**
- После добавления новых фото обязательно запускайте `./update-images.sh`
- Не редактируйте `gallery.json` вручную - он создается автоматически

---

## Быстрый старт

```bash
# 1. Добавьте фото в папку images/
cp ~/Desktop/мои-фото/*.jpg images/

# 2. Обновите галерею
./update-images.sh

# 3. Откройте сайт
open index.html
```

Всё! Больше ничего делать не нужно! 🎉
